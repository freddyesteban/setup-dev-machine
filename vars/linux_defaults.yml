gcloud_version: 375.0.0
go_version: 1.17.8
kind_version: 0.12.0
kubectl_version: 1.22.2 # comment out for latest
helm_version: 3.8.1
helm2_version: 2.17.0
minikube_version: 1.25.2
terraform_version: 1.2.1
docker_compose_version: 2.4.0

# languages
node_version: 16.14.0
python_version: 3.9.12
ruby_version: 3.1.1

# Allows to install packages for non-lts installation where binaries are not available for latest version
# or if using a non-ubuntu/non-debian distro, e.g. Pop OS, the ansible_distribution would be "Pop!_OS" which
# will not be found under the name
distro_codename: impish

# # Use system python because in linux, pyenv python is missing packages from apt.
# # This should work for both Linux and MacOS.
# ansible_python_interpreter: /usr/bin/python3


# # Kubernetes development
# minikube_kubernetes_version: 1.17.16
# minikube_driver: virtualbox

# # MongoDB version only use major.minor format
# mongodb_version: 4.4

